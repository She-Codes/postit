<div class="row">
  <div class="col-lg-9">
    <%= render 'shared/content_title', title: 'Dashboard' %>
  </div><!--end .col-lg-9 -->
</div><!-- end .row -->

<div class="row">
  <div class="col-lg-9">
    <%= form_tag '/admin/users', method: 'post' do %>
      <table class="table">
        <thead>
          <tr>
            <th>User</th>
            <th>Role</th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <%= fields_for "users[]", user, {index: user.id} do |f| %>
              <tr>
                <td><%= user.username %></td>
                <td>
                  <%= f.select(:role, [['user', 'user'],
                    ['moderator', 'moderator'], ['admin', 'admin']]) %>
                </td>
              </tr>
            <% end %><!-- end fields_for -->
          <% end %><!-- end @users loop -->
        </tbody>
      </table>
      <%= submit_tag "Update" %>
    <% end %>
  </div><!--end .col-lg-9-->
</div><!--end .row-->